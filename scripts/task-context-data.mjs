// Data for `pnpm task:context <area>`. Paths listed under `docs` must exist —
// `pnpm check:agents` enforces it; `code`/`tests` entries may be planned.

export const shared = {
  avoid: ["pnpm-lock.yaml", "drizzle/", ".next/", "playwright-report/", "test-results/"],
  rules: [
    "Read tests before implementation when determining intended behavior.",
    "Keep routes thin; put framework-free domain rules in src/lib or the feature module.",
    "Run the narrowest useful test while iterating, then pnpm check before commit.",
    "Update docs in the same change when behavior, architecture, or domain language changes.",
  ],
};

export const areas = {
  waivers: {
    goal: "Extend the waiver flow without weakening its immutable signed history or fail-closed medical referral states.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
      "docs/architecture/overview.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/client.ts",
      "src/db/bookings.ts",
      "src/lib/authz.ts",
      "src/app/waivers/[token]",
      "src/app/shop/[shopSlug]/waivers",
    ],
    tests: ["src/db/bookings.test.ts", "e2e"],
    invariants: [
      "Tenant-scope every record and query.",
      "Signed records are immutable; corrections create a new version.",
      "Referral-triggering medical answers fail closed.",
      "Submission must be idempotent and authorized.",
    ],
    validate: [
      "pnpm test src/db/bookings.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e --reporter=line",
    ],
  },
  certifications: {
    goal: "Build certification evidence and a fail-closed readiness result shared by staff, divers, and manifests.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
      "docs/architecture/overview.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/readiness.ts",
      "src/lib/readiness.ts",
      "src/app/shop/[shopSlug]/divers/[personId]",
      "src/app/shop/[shopSlug]/trips/[id]",
    ],
    tests: ["src/lib/readiness.test.ts", "src/db/readiness.test.ts", "e2e"],
    invariants: [
      "Requirements and evidence are separate; missing configuration fails closed.",
      "Only verified, unexpired evidence at or above the required level can clear a diver.",
      "Every readiness blocker is typed and human-readable.",
      "Tenant-scope card capture, review, and readiness queries.",
    ],
    validate: [
      "pnpm test src/lib/readiness.test.ts src/db/readiness.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e --reporter=line",
    ],
  },
  "rental-fit": {
    goal: "Maintain rental fit and the derived per-trip prep list (tanks, kit, nitrox split).",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
      "docs/architecture/overview.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/rental-fit.ts",
      "src/db/nitrox.ts",
      "src/lib/dive-prep.ts",
      "src/app/shop/[shopSlug]/trips/[id]/prep",
    ],
    tests: ["src/lib/dive-prep.test.ts", "src/db/nitrox.test.ts", "e2e/nitrox.spec.ts"],
    invariants: [
      "DiveDay tracks no equipment inventory: a fit is a size record, never an allocation.",
      "One tank per diver per planned dive — the count is never short of the dive plan.",
      "A nitrox request requires a verified card at write time, and is re-checked on every read.",
      "A diver with no fit on file is named on the prep list, never silently omitted.",
    ],
    validate: [
      "pnpm test src/lib/dive-prep.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e e2e/nitrox.spec.ts --reporter=line",
    ],
  },
  manifests: {
    goal: "Extend manifests and roll call without weakening the shared fail-closed readiness result they display and act on.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
      "docs/architecture/overview.md",
      "docs/architecture/decisions/20260718-offline-manifest-snapshots.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/readiness.ts",
      "src/db/manifests.ts",
      "src/lib/readiness.ts",
      "src/lib/manifests.ts",
      "src/lib/offline-manifests.ts",
      "src/lib/offline-manifest-store.ts",
      "src/app/shop/[shopSlug]/trips/[id]/manifest",
      "src/app/offline-manifest",
    ],
    tests: [
      "src/db/readiness.test.ts",
      "src/db/manifests.test.ts",
      "src/lib/manifests.test.ts",
      "src/lib/offline-manifests.test.ts",
      "e2e/manifest.spec.ts",
    ],
    invariants: [
      "Every active booking is represented, including incomplete or blocked records.",
      "Readiness stays fail-closed and is never recomputed differently in the manifest UI.",
      "Boarding changes are explicit, time-stamped, tenant-scoped, and auditable.",
      "Offline copies are encrypted, explicit, freshness-labeled, expiring, and never an editable roster.",
      "Offline events are idempotent; live readiness is rechecked and newer server history wins.",
      "Departure and every after-dive checkpoint keep independent roll-call state.",
      "Phone/sunlight use requires large controls, text labels, and no color-only status.",
    ],
    validate: [
      "pnpm test src/db/readiness.test.ts src/db/manifests.test.ts src/lib/manifests.test.ts src/lib/offline-manifests.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e --reporter=line",
    ],
  },
  nitrox: {
    goal: "Extend nitrox fill logging without weakening the verified-card gate or the derived, fail-closed MOD.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/product/human-decisions.md",
      "docs/design/principles.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/nitrox.ts",
      "src/db/nitrox.test.ts",
      "src/lib/nitrox.ts",
      "src/app/shop/[shopSlug]/trips/[id]/prep",
    ],
    tests: ["src/lib/nitrox.test.ts", "src/db/nitrox.test.ts", "e2e"],
    invariants: [
      "Only a verified nitrox card lets a diver receive an EANx fill; the gate is enforced at write time.",
      "Only a valid recreational EANx mix (22–40% O2) is accepted; out-of-band values fail closed.",
      "MOD is derived from the mix and ppO2 ceiling, never taken from the caller.",
      "Tenant-scope every card, tank, and fill; a fill is append-only evidence.",
      "Nitrox is a safety-critical surface — it needs a dive-domain-expert review (V-05).",
    ],
    validate: [
      "pnpm test src/lib/nitrox.test.ts src/db/nitrox.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e --reporter=line",
    ],
  },
  courses: {
    goal: "Extend the course catalog, sessions, enrollment gates, and public course pages.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
      "docs/architecture/overview.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/courses.ts",
      "src/db/course-paths.ts",
      "src/db/course-templates.ts",
      "src/lib/courses.ts",
      "src/lib/course-inquiry.ts",
      "src/app/shop/[shopSlug]/courses",
    ],
    tests: [
      "src/lib/courses.test.ts",
      "src/db/courses.test.ts",
      "src/db/course-paths.test.ts",
      "e2e/courses.spec.ts",
      "e2e/course-paths.spec.ts",
    ],
    invariants: [
      "An instructor-led session fails closed when it has no instructor.",
      "Enrollment cert gates come from the course baseline and clear only on verified evidence.",
      "Course slugs never shadow staff routes; slug lookups are shop-scoped.",
      "Public course pages are allowlisted in isPublicShopRoute; every other course surface stays auth-gated.",
      "Content edits never touch pricing, the cert gate, or the agency age baseline.",
      "A certification path is guidance, never a gate: it changes what a diver is shown next, never who may enrol.",
    ],
    validate: [
      "pnpm test src/lib/courses.test.ts src/db/courses.test.ts src/db/course-paths.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e e2e/courses.spec.ts e2e/course-paths.spec.ts --reporter=line",
    ],
  },
  today: {
    goal: "Maintain the Today work queue: urgency ranking, blocker collapse, and actionable next steps.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
    ],
    code: [
      "src/lib/today.ts",
      "src/db/today.ts",
      "src/lib/blockers.ts",
      "src/db/blockers.ts",
      "src/app/shop/[shopSlug]/page.tsx",
    ],
    tests: ["src/lib/today.test.ts", "src/db/today.test.ts", "src/lib/blockers.test.ts"],
    invariants: [
      "The earlier departure's problems come first; severity ranks only within one departure.",
      "Undated work sorts last — never presented as urgent.",
      "Identical blockers on one boat collapse into a single batch action; different blockers or boats never merge.",
      "A diver who is already clear produces no action.",
      "An action acts in place or points at the record where the work happens — never a dead end.",
    ],
    validate: ["pnpm test src/lib/today.test.ts src/db/today.test.ts --reporter=dot", "pnpm check"],
  },
  design: {
    goal: "Deliver a calm, clear, accessible interface that follows DiveDay's semantic design system.",
    docs: [
      "docs/design/principles.md",
      "docs/product/vision.md",
      "docs/product/features/roadmap.md",
      "docs/architecture/decisions/0004-design-tokens.md",
    ],
    code: ["src/app/globals.css", "src/app", "src/components"],
    tests: ["e2e"],
    invariants: [
      "Use semantic tokens only; no raw colors in components.",
      "Inspect light/dark and phone/desktop output.",
      "Include empty, loading, validation, error, and success states.",
      "Keep touch targets and focus states accessible.",
    ],
    validate: ["pnpm lint", "pnpm typecheck", "pnpm visual", "pnpm e2e --reporter=line"],
  },
  "brand-voice": {
    goal: "Create consistent DiveDay brand, voice, visual, merch, and vendor collateral from the current identity.",
    docs: [
      "docs/design/brand.md",
      "docs/design/principles.md",
      "docs/product/vision.md",
      "docs/product/marketing.md",
    ],
    code: ["src/app/globals.css", "src/app/layout.tsx", "src/components/Logo.tsx"],
    tests: [],
    invariants: [
      "The product name is DiveDay: one word, two capitals.",
      "Use the current lagoon, coral, sand, ocean, ink, and Geist system rather than inventing a parallel identity.",
      "Coral is a restrained accent; feedback colors are not general brand colors.",
      "Public and sales copy remains truthful and shipped-only under the marketing claims policy.",
      "New brand directions are labeled proposed until product-owner approval.",
    ],
    validate: ["pnpm check:docs", "pnpm check:agents"],
  },
  database: {
    goal: "Change persistence safely while preserving tenant, capacity, and transactional invariants.",
    docs: [
      "docs/architecture/overview.md",
      "docs/architecture/decisions/0005-database.md",
      "docs/engineering/testing.md",
      "docs/product/glossary.md",
    ],
    code: [
      "src/db/schema.ts",
      "src/db/client.ts",
      "src/db/trips.ts",
      "src/db/shops.ts",
      "src/db/bookings.ts",
      "src/db/seed.ts",
    ],
    tests: ["src/db", "src/lib"],
    invariants: [
      "src/db/schema.ts is the source of truth; never infer schema from generated migrations.",
      "Every tenant-owned domain row carries shop_id.",
      "Capacity enforcement remains transactional.",
      "Schema changes include focused PGlite tests and generated migrations.",
    ],
    validate: [
      "pnpm test src/db --reporter=dot",
      "pnpm typecheck",
      "pnpm db:generate",
      "pnpm check",
    ],
  },
  bookings: {
    goal: "Extend booking behavior without weakening capacity, authorization, or user-facing recovery states.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/design/principles.md",
      "docs/architecture/overview.md",
    ],
    code: [
      "src/db/bookings.ts",
      "src/db/bookings.test.ts",
      "src/lib/trips.ts",
      "src/app/shop/[shopSlug]/schedule",
      "src/app/shop/[shopSlug]/trips",
    ],
    tests: ["src/db/bookings.test.ts", "e2e"],
    invariants: [
      "Capacity enforcement is transactional.",
      "Cancelled and past trips cannot accept bookings.",
      "Tenant and staff authorization checks stay explicit.",
    ],
    validate: [
      "pnpm test src/db/bookings.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e --reporter=line",
    ],
  },
  auth: {
    goal: "Change authentication or authorization without creating edge/server divergence or tenant leakage.",
    docs: [
      "docs/architecture/decisions/0006-auth.md",
      "docs/architecture/overview.md",
      "docs/engineering/testing.md",
    ],
    code: [
      "src/lib/auth.config.ts",
      "src/lib/auth.ts",
      "src/lib/authz.ts",
      "src/lib/session.ts",
      "src/proxy.ts",
    ],
    tests: ["src/lib", "e2e"],
    invariants: [
      "Edge configuration remains edge-safe.",
      "Server actions and queries enforce authorization independently of route gating.",
      "Dev credentials never become production behavior.",
    ],
    validate: ["pnpm test src/lib --reporter=dot", "pnpm typecheck", "pnpm e2e --reporter=line"],
  },
  payments: {
    goal: "Extend Stripe Connect checkout, orders, invoicing, and refunds without breaking idempotency or currency/tenant guarantees. Security-sensitive (AGENTS.md Hard rules) — changes need a security-reviewer pass before merge.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/architecture/overview.md",
      "docs/architecture/decisions/20260719-stripe-connect-orders.md",
      "docs/architecture/decisions/20260721-checkout-at-booking.md",
      "docs/architecture/decisions/20260723-payment-operation-intents.md",
    ],
    code: [
      "src/lib/payments/checkout.ts",
      "src/lib/payments/connect.ts",
      "src/lib/payments/invoicing.ts",
      "src/lib/payments/promotions.ts",
      "src/lib/payments/webhook.ts",
      "src/db/orders.ts",
      "src/db/payments.ts",
      "src/db/checkouts.ts",
      "src/db/refunds.ts",
      "src/db/stripe-accounts.ts",
    ],
    tests: [
      "src/lib/payments/checkout.test.ts",
      "src/lib/payments/connect.test.ts",
      "src/lib/payments/invoicing.test.ts",
      "src/lib/payments/promotions.test.ts",
      "src/lib/payments/webhook.test.ts",
      "src/db/orders.test.ts",
      "src/db/payments.test.ts",
      "src/db/checkouts.test.ts",
      "src/db/refunds.test.ts",
      "src/db/stripe-accounts.test.ts",
      "e2e",
    ],
    invariants: [
      "Security-sensitive: payments changes need a security-reviewer pass before merge (AGENTS.md Hard rules).",
      "Every Stripe-mutating call carries a deterministic Idempotency-Key derived from a durable payment_operation_intents row written before the call, so a crash or retry converges on one Stripe object instead of minting a second.",
      "Paid state comes only from Stripe's own answer — a webhook or an authenticated status read — never from a return-URL claim.",
      "A booking's payment status only ever advances forward: setBookingPaymentIfNotFinal refuses to write a lesser status over a refunded or waived row, and a cancelled booking can never be marked paid.",
      "Money in a zero-decimal currency is never divided by 100, and a settled order or checkout keeps its own currency after the shop later switches.",
      "A cancellation refund automates only inside the trip's stated window and only for a real Stripe payment; a counter payment, an unstated window, or a Stripe not_refundable mismatch routes to staff instead of silently dropping the case.",
      "A duplicate or replayed webhook is idempotent: it repairs a partial write but never regresses an already-refunded booking back to paid.",
      "Every read and mutation is tenant-scoped; another shop can neither see nor act on an order, checkout, or refund.",
    ],
    validate: [
      "pnpm test src/lib/payments/checkout.test.ts src/lib/payments/connect.test.ts src/lib/payments/invoicing.test.ts src/lib/payments/promotions.test.ts src/lib/payments/webhook.test.ts src/db/orders.test.ts src/db/payments.test.ts src/db/checkouts.test.ts src/db/refunds.test.ts src/db/stripe-accounts.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e e2e/orders-demo.spec.ts e2e/refunds.spec.ts --reporter=line",
    ],
  },
  notifications: {
    goal: "Extend email/SMS/WhatsApp notification delivery without breaking idempotency, per-recipient locale, or delivery-status accuracy.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/engineering/resend-email-runbook.md",
      "docs/engineering/whatsapp-cloud-api-runbook.md",
      "docs/engineering/sms-delivery-receipts-runbook.md",
      "docs/architecture/decisions/20260718-notification-delivery-status.md",
      "docs/architecture/decisions/20260720-notification-attempt-history.md",
      "docs/architecture/decisions/20260731-per-person-notification-locale.md",
      "docs/architecture/decisions/20260802-whatsapp-embedded-signup.md",
      "docs/architecture/decisions/20260802-sms-delivery-receipts-in-cloudformation.md",
    ],
    code: [
      "src/lib/notifications/index.ts",
      "src/lib/notifications/email.ts",
      "src/lib/notifications/sms.ts",
      "src/lib/notifications/whatsapp.ts",
      "src/lib/notifications/whatsapp-signup.ts",
      "src/lib/notifications/whatsapp-events.ts",
      "src/lib/notifications/sms-events.ts",
      "src/lib/notifications/courtesy.ts",
      "src/lib/notifications/events.ts",
      "src/lib/notifications/webhook.ts",
      "src/lib/secret-box.ts",
      "src/db/notifications.ts",
      "src/db/whatsapp-accounts.ts",
      "src/app/api/webhooks/resend",
      "src/app/api/webhooks/whatsapp",
      "src/app/api/webhooks/sms",
    ],
    tests: [
      "src/lib/notifications/index.test.ts",
      "src/lib/notifications/email.test.ts",
      "src/lib/notifications/sms.test.ts",
      "src/lib/notifications/whatsapp.test.ts",
      "src/lib/notifications/whatsapp-signup.test.ts",
      "src/lib/notifications/whatsapp-events.test.ts",
      "src/lib/notifications/sms-events.test.ts",
      "src/lib/notifications/courtesy.test.ts",
      "src/lib/notifications/events.test.ts",
      "src/lib/notifications/webhook.test.ts",
      "src/lib/secret-box.test.ts",
      "src/db/notifications.test.ts",
      "src/db/whatsapp-accounts.test.ts",
    ],
    invariants: [
      "notify() is the one send entrypoint; every notification kind carries its own deterministic idempotency key so a retried send never double-delivers.",
      "A reserved test/demo recipient domain never reaches Resend, even inside a mixed batch.",
      "Delivery fails closed: a provider rejection surfaces as failure without exposing provider details, and a 429 retries using Retry-After while keeping the same idempotency key.",
      "Every diver-facing notification is sent and resent in the recipient's own recorded locale, falling back to the shop's default, never the sending staff member's language.",
      "Delivery-attempt history is append-only; a provider-reported event applies only when newer than the stored outcome, and an unknown message id is reported without failing so Resend stops retrying it.",
      "Resend webhook signatures are HMAC- and timestamp-verified before any event is trusted; a stale, tampered, or malformed payload is rejected.",
      "The courtesy text goes out on the shop's own WhatsApp when connected and on platform SMS otherwise — one channel per diver per message, never both, and any WhatsApp failure falls back to SMS.",
      'A shop connects WhatsApp only through Meta Embedded Signup — DiveDay never asks for a pasted credential, and the surface says "coming soon" until Meta approves the app.',
      "Meta webhook signatures are verified over the raw request body before any event is trusted; a re-serialized payload never matches what Meta signed.",
      "A delivery event that names a tenant is applied scoped to it — a WhatsApp event resolves its WABA to a shop and never touches another shop's row on a provider message id alone.",
      "SMS delivery receipts arrive over the same signed SNS envelope the SES webhook verifies, because SNS has no delivery webhook of its own for a direct-to-phone publish.",
      "A shop's WhatsApp access token is encrypted at rest and never readable back out of src/db/whatsapp-accounts.ts — the only export path is a provider that has already closed over it.",
    ],
    validate: [
      "pnpm test src/lib/notifications src/lib/secret-box.test.ts src/db/notifications.test.ts src/db/whatsapp-accounts.test.ts --reporter=dot",
      "pnpm check",
    ],
  },
  reviews: {
    goal: "Extend verified diver reviews and discount codes (shop-wide promos + last-minute trip fills) without weakening the review provenance/moderation gate or the redeemability checks Stripe actually enforces.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/architecture/overview.md",
      "docs/architecture/decisions/20260729-verified-diver-reviews.md",
      "docs/architecture/decisions/20260729-shop-promo-codes.md",
      "docs/architecture/decisions/20260727-last-minute-fill-promos.md",
    ],
    code: [
      "src/lib/reviews.ts",
      "src/lib/promo-codes.ts",
      "src/db/reviews.ts",
      "src/db/shop-promos.ts",
      "src/db/trip-promos.ts",
    ],
    tests: [
      "src/lib/reviews.test.ts",
      "src/lib/promo-codes.test.ts",
      "src/db/reviews.test.ts",
      "src/db/shop-promos.test.ts",
      "src/db/trip-promos.test.ts",
      "e2e/reviews.spec.ts",
      "e2e/promo-codes.spec.ts",
    ],
    invariants: [
      "A review only ever comes from a diver's own post-trip recap link, and only for a booking that actually dived — never a cancelled or no-show one.",
      "A bare rating publishes immediately; a review carrying words is held for staff release, and adding words to an already-published review sends it back for moderation.",
      "A published rating average never counts a held (words-pending) review, and never rounds a mixed record up to a clean score.",
      "Every review and promo read/write stays shop-scoped; another shop can neither see nor moderate it, nor redeem or manage its codes.",
      "discountedAmountCents mirrors Stripe's own rounding so a quoted discount matches what Stripe actually charges; a code that never minted at Stripe, is disabled, or is out of window/scope is not redeemable.",
      "A promo redemption is counted once per paid checkout, never twice on a replayed webhook.",
      "A last-minute trip promo's max_redemptions is capped at the trip's open-seat count and never discounts a full trip.",
    ],
    validate: [
      "pnpm test src/lib/reviews.test.ts src/lib/promo-codes.test.ts src/db/reviews.test.ts src/db/shop-promos.test.ts src/db/trip-promos.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e e2e/reviews.spec.ts e2e/promo-codes.spec.ts --reporter=line",
    ],
  },
  "data-portability": {
    goal: "Extend CSV export/import without weakening tenant scoping or the verification gates on imported evidence. Security-sensitive (AGENTS.md Hard rules) — changes need a security-reviewer pass before merge.",
    docs: [
      "docs/product/features/roadmap.md",
      "docs/product/glossary.md",
      "docs/architecture/decisions/20260724-export-bundled-photos.md",
      "docs/architecture/decisions/20260724-import-verified-cards.md",
      "docs/architecture/decisions/20260724-import-waiver-acceptance.md",
      "docs/architecture/decisions/20260725-import-prior-visits.md",
    ],
    code: ["src/db/export.ts", "src/db/import.ts"],
    tests: [
      "src/db/export.test.ts",
      "src/db/import.test.ts",
      "e2e/export.spec.ts",
      "e2e/import.spec.ts",
    ],
    invariants: [
      "Security-sensitive: export/import changes need a security-reviewer pass before merge (AGENTS.md Hard rules).",
      "Every schema table and every column of an exported table is either exported, folded into another file, or deliberately excluded — an untracked new table/column fails the coverage test.",
      "Export/import access is re-checked against the database at request time, not trusted from the JWT; access is revoked the instant an accountable role or login is removed, before any token would expire.",
      "An export never leaks another shop's rows, and an import never sees or matches another shop's divers or card numbers.",
      "An imported card lands verified-and-flagged, never fully trusted outright; a card number already held by a different diver is reported, not silently reassigned, and writes nothing.",
      "An imported waiver claim never overrides a live medical-review hold, however the row is dated, and a waiver document link that resolves to a blocked/private address is dropped without failing the whole row.",
      "Imported prior-visit history writes an inert, deduplicated history row per booking and never touches an operational table — the migration can never reach the dock.",
    ],
    validate: [
      "pnpm test src/db/export.test.ts src/db/import.test.ts --reporter=dot",
      "pnpm check",
      "pnpm e2e e2e/export.spec.ts e2e/import.spec.ts --reporter=line",
    ],
  },
};
