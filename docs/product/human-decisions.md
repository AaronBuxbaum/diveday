# Human decision log

The durable worklist for decisions, approvals, and verification that need a human owner. It
complements the delivery sequence in [roadmap.md](features/roadmap.md): implementation can advance only
when the relevant row is **Chosen** or intentionally **Deferred**.

## How to use this log

1. The product owner assigns a named human owner and records the outcome and date in the relevant
   row. Link the approval, policy, or test evidence when it exists.
2. An implementer translates a product or technical outcome into the roadmap and, when it is
   significant and hard to reverse, an ADR. Then change the row to **Implemented**.
3. Do not mark a safety or production row complete based on a local demo. Record the required
   operational or legal evidence first.
4. The *how* of the stakeholder conversations that clear these rows — who to contact, briefing
   packets, agendas, question lists — lives in the [stakeholder playbooks](stakeholders/README.md).
   Status stays here; playbooks never hold it.

**Status key:** **Ready** needs a human action now; **Deferred** is deliberately not needed for the
current slice; **Chosen** has an outcome awaiting implementation; **Implemented** is in the product
but still may need validation; **Validated** has the stated evidence.

## Immediate actions for the product owner

1. Name the operating jurisdiction(s) and the person responsible for legal approval. Have them
   review the waiver language, medical questions, typed-consent standard, and evidence-retention
   policy before production use.
2. Vercel is the selected production host and Neon (via Vercel's Marketplace integration) is the
   selected Postgres provider, in the **US-East / Washington, D.C.** region. Production builds run
   migrations automatically (see [Neon hosting
   ADR](../architecture/decisions/20260718-vercel-neon-hosting.md)). Owner named 2026-07-24:
   **Aaron Buxbaum** (sole operator) owns production secrets, backups, domain, and incident response
   (H-04). Still to write down: the backup/restore-test cadence and incident runbook.
3. Assign the dive operations lead and run V-02 against the encrypted offline manifest before a
   production departure. Record the exact phone/browser, glare and wet-hand findings, airplane-mode
   reload result, multi-checkpoint roll call, conflict reconciliation, and print fallback.
4. Run the full browser verification for the merged gear and manifest work in light/dark desktop and
   phone viewports, keeping the automated offline Playwright scenario as regression evidence rather
   than a substitute for outdoor field validation.

## Decision register

| ID | Status | Human owner | Decision or approval needed | Minimum outcome to record | Unblocks / follow-up |
| --- | --- | --- | --- | --- | --- |
| H-01 | Ready | Product owner + scuba-liability specialist (Tier 3 — see [legal-engagement-scope.md](stakeholders/legal-engagement-scope.md)) | Which jurisdiction(s), waiver text, and medical-question wording apply to the shop? | Jurisdiction, approved template/version, reviewer, and approval date. | **Jurisdiction confirmed 2026-07-30 (Aaron Buxbaum): Florida is the launch jurisdiction**, not just a planning assumption. Row stays **Ready** — approved waiver text, medical-question wording, and the Tier 3 specialist review are still outstanding. Production waiver templates and any jurisdiction-specific questionnaire. The current PADI-style form shape is only a [provisional baseline](#waiver-and-signature). See [waiver ADR](../architecture/decisions/20260718-waiver-signature-retention.md). |
| H-02 | Ready | Product owner + generalist attorney (drafts) + scuba-liability specialist (confirms) — Tier 2, see [legal-engagement-scope.md](stakeholders/legal-engagement-scope.md#tier-2--generalist-drafts-specialist-blesses-before-it-goes-live) | What evidence-retention period, deletion-request process, and backup/audit exception apply to waivers and medical flags? | Retention duration, deletion workflow, permitted staff access, and any legal hold or audit exception. | **Working default recorded 2026-07-30 (Aaron Buxbaum): retain waiver/medical records indefinitely for now**, noted here for review once counsel is engaged — not a final retention policy. Row stays **Ready**: the attorney draft + specialist confirmation (deletion-request process, permitted staff access, legal hold/audit exception) are still outstanding, and indefinite retention of medical data may itself need revisiting once that review happens. Production data lifecycle, access controls, and deletion tooling. |
| H-03 | Ready | Product owner + generalist attorney (general UETA/E-SIGN read) + scuba-liability specialist (adventure-sport-sufficiency layer) — Tier 2, see [legal-engagement-scope.md](stakeholders/legal-engagement-scope.md#tier-2--generalist-drafts-specialist-blesses-before-it-goes-live) | Is the current typed name + explicit consent + timestamp sufficient for the intended release, or is a specialist e-signature provider required? | Accepted assurance level, required provider criteria (if any), and rollout boundary. | **Provider sub-decision recorded 2026-07-24:** keep the local signature provider (typed name + explicit consent + timestamp) for now — no specialist e-signature vendor for the current slice; the vendor-adapter option stays available behind the provider seam if a later review requires it. **This does not clear the gate:** whether typed consent is a legally *sufficient* assurance level for the intended release is still undecided and must be signed off with H-01/H-02 and V-03 before production. Row stays **Ready** until that assurance outcome is recorded. Baseline documented in the [provisional defaults](#waiver-and-signature). |
| H-04 | Implemented | Product owner / technical owner | Vercel is selected for web hosting and Neon (Vercel Marketplace integration) is selected as the Postgres provider. Still open: who owns secrets, backups, domain, and incident response? | Provider: Neon. Driver: `drizzle-orm/node-postgres`. Production builds run `pnpm db:migrate`; Vercel System Environment Variables are enabled. Still needed: region confirmation and named owner for secrets/backups/incident response. | **Recorded 2026-07-24:** production region is **Washington, D.C. / US-East** (Neon US East, N. Virginia — `aws-us-east-1`). **Aaron Buxbaum** is the named owner of production secrets, backups, domain, and incident response — the sole operator today, so the "product owner / technical owner" split collapses to one person; re-assign when the team grows. Still to write down (not blocking): the concrete backup cadence/restore-test and incident-response runbook. See [Neon hosting ADR](../architecture/decisions/20260718-vercel-neon-hosting.md) and [Vercel hosting ADR](../architecture/decisions/20260718-vercel-hosting.md). |
| H-05 | Implemented | Product owner + dive operations lead | The product now uses explicit encrypted device snapshots rather than a live-only pilot. Approve or replace its 15-minute current / four-hour aging thresholds and retention at the earlier of 14 days after save or seven days after trip end. | Named operations/privacy owners, accepted thresholds and retention, and the production stop rule for a missing/expired/corrupt device copy. | **Approved as-is 2026-07-24 (Aaron Buxbaum, acting operations/privacy owner):** the 15-minute current / four-hour aging thresholds and the retention at the earlier of 14 days after save or seven days after trip end are accepted unchanged. Production stop rule: a missing, expired, or corrupt device copy is **not** a boarding source — staff fall back to a fresh live load or the print/PDF manifest and never board from stale/absent data. **Still gated on V-02** outdoor field evidence before a real departure relies on the offline copy. See [offline manifest ADR](../architecture/decisions/20260718-offline-manifest-snapshots.md). **Controls consolidated 2026-07-26 (Aaron Buxbaum):** device copies now save and refresh themselves automatically (on load, reconnect, tab focus, and a five-minute interval), with one remaining manual "Refresh now" control, and the staff-delete button is removed — expiry (unchanged: earlier of 14 days after save or 7 days after trip end) is the only way a copy goes away. Thresholds, retention duration, and the missing/expired/corrupt-copy stop rule are otherwise unchanged. See [manifest offline-copy automation ADR](../architecture/decisions/20260726-manifest-offline-copy-automation.md). |
| H-06 | Implemented | Product owner + dive operations lead | What is the initial gear policy for sizing, preferences, staff assignment, and substitutions? | Required measurements/preferences, who may override a request, and the safe fallback when a size is unavailable. | **Chosen 2026-07-24 (default-kit membership), refined 2026-07-24 (pricing):** the **dive computer defaults on for every diver** and is in the default rental catalog, but is **priced on its own line, not folded into the "set" bundle** (`src/lib/rentals.ts`; the GoPro is likewise a default-off add-on). The owner's first cut folded it into the set; on review that was reversed so a diver who brings their own computer keeps the full-set discount on the five hard-goods pieces — being on by default is separate from being part of the priced set. Shipped with tests and the [rental gear request](#rental-gear-request) note updated. **Measurement method decided 2026-07-24 (Aaron Buxbaum):** the booking rental request captures self-reported BCD/wetsuit size, boot/fin size, and usual weighting as a **planning input only** — never a reservation or fit authorization — with staff confirming true fit at check-in (already the shape of the [rental gear request](#rental-gear-request)). **Substitution authority + safe fallback: decided, in implementation** — any staff member on the trip may substitute a real available item (logged), but *overriding/denying* a diver's stated request is reserved to instructor/divemaster/manager (rides on the H-14 role matrix), and when a requested size is unavailable the request drops to a **"needs staff fit"** state rather than auto-assigning a different size. **Implemented 2026-07-24:** the override gate is `canOverrideGearRequest` (owner/manager/instructor/**divemaster** — wider than `canConfigureTrips` on purpose, since sizing a diver is in-water judgement), enforced in both layers per ADR-0006; the fallback is a first-class **"needs staff fit"** state on `rental_fit_profiles`. The flagged diver keeps their line and count on the prep list — the count is what the packer loads from, so dropping it arrives a BCD short with nothing to fit them from — but their *sized* pieces carry no size, reading "fit at check-in", because packing a substitute is the thing it prevents. Unsized kit (regulator, computer, GoPro), weights, and tanks are untouched: none has a stock size to be short of. They are named in their own check-in section along with the sizes they asked for, and the flag is sticky against size edits. **Raising** the flag stays open to every staff member — it escalates to a person rather than overwriting the diver's request — while **clearing** it takes the override gate, since that asserts the stated size packs after all. Recording a diver's *first* fit is open too: there is nothing on file to override. See [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md). *(This decision arrived labelled "H-05" but its content — the default kit — belongs to this gear-policy row; register H-05 is the offline-manifest thresholds.)* |
| H-07 | In progress | Product owner + finance owner | What payment/deposit, cancellation, refund, tax, and provider policy should the first paid booking support? | Policy plus provider approval and webhook/account owner. | The provider decision is made and implemented: Stripe Connect (Standard, shop-owned accounts), orders/invoices, and webhook confirmation — see [20260719-stripe-connect-orders](../architecture/decisions/20260719-stripe-connect-orders.md). Public checkout-at-booking is also implemented on that substrate with a [provisional policy](#booking-checkout) — see [20260721-checkout-at-booking](../architecture/decisions/20260721-checkout-at-booking.md). The deposit and declarative-cancellation-window *mechanisms* are now shipped too (opt-in, off by default, no default values) — see [20260721-deposit-cancellation-policy](../architecture/decisions/20260721-deposit-cancellation-policy.md). **Automated refunds inside the cancellation window are now implemented** — a cancel inside a shop's stated window refunds a Stripe payment automatically through the shop's own account, degrading to the staff-run refund for counter payments, disconnected accounts, or Stripe failures — see [20260721-automated-cancellation-refund](../architecture/decisions/20260721-automated-cancellation-refund.md). Still open (policy, not mechanism): the deposit/window *values* shops should be guided toward, percentage vs. flat deposits (mechanism deferred by owner request), refund/tax policy, and whether the platform ever takes a fee; a live Stripe Connect platform application (`STRIPE_CONNECT_CLIENT_ID`) and Connect webhook secret (`STRIPE_WEBHOOK_SECRET`) — now also subscribed to the three `checkout.session.*` events — still need a named owner. **Values decided 2026-07-24 (Aaron Buxbaum):** DiveDay ships **no hard defaults** (correct as-is) but now shows a non-binding **guidance hint** on the trip deposit/cancellation fields ("many shops set 20–30% of the fare"; "48 hours is a common window") — `trips/new/page.tsx` and `DetailsSection.tsx`. Deposits stay **flat** (percentage remains deferred). **No platform fee** for the founding cohort. Unpaid bookings **do not auto-expire** — they surface via the existing `payment_due` blocker for staff to release. The Stripe Connect platform application and webhook-secret owner is **Aaron Buxbaum** (sole operator; same as H-04). **Still gated on the accountant:** sales-tax/VAT treatment on deposits and fares — excluded from this decision. **Reconfirmed 2026-07-30 (Aaron Buxbaum): no baked-in default deposit or cancellation-window values, ever** — the guidance-hint copy is the final answer, not an interim step toward a real default; a shop with neither set stays full-fare, no-refund-window (current behavior, now explicitly settled rather than open). |
| H-08 | In progress | Product owner + operations lead | Which certification levels, agency rules, and Discover Scuba Diver rules define a course booking? Separately: the glossary's own **C-card** entry states real certification cards do not expire — should the `certifications`/`specialty_certifications` "expiry" field be relabeled and presented to staff as a shop-set "refresher due" date instead of an implied card expiry (see [20260723-certification-expiry-date-only](../architecture/decisions/20260723-certification-expiry-date-only.md), flagged by a dive-domain-expert review of CR-009)? | Supported courses, prerequisites, expiration/verification rules, ratios, and exception process. Plus: whether/how to relabel the expiry field and its staff-facing copy. | **Relabel decided + shipped 2026-07-24:** the optional card date is now presented to staff as a shop-set **"refresher due"** date, not a card expiry — add-card form label, diver-profile line ("refresher due/overdue"), badge, and the cert/specialty readiness blockers all reworded, with the glossary and [certification-expiry ADR](../architecture/decisions/20260723-certification-expiry-date-only.md) updated. Stored column, gating, and readiness codes unchanged (fails closed exactly as before); CSV `expires_at` data name kept for round-trip. **Course baseline approved as the starting point 2026-07-24 (Aaron Buxbaum):** the [conservative provisional rules](#course-admission) — DSD/OW ungated, AOW/refresher requiring a verified Open Water card, instructor-led sessions blocked until an instructor is assigned — are accepted as the operating baseline. **Ratios resolved + shipped 2026-07-24 (Aaron Buxbaum: use PADI/SSI's own published standards directly, consistent enough across agencies to trust):** the entry-level (DSD/Open Water) in-water ratio — 8 students per instructor, +2 per certified assistant, ceiling 12 — is now a real booking gate on any ungated course session (`src/lib/course-ratios.ts`, [20260724-course-admission-standards](../architecture/decisions/20260724-course-admission-standards.md)), sourced and cited there; continuing-ed courses aren't ratio-capped, matching PADI's own published scope. **Minimum age resolved + shipped 2026-07-24 (Aaron Buxbaum chose option B — collect a date of birth, fail open):** `people.date_of_birth` is now an optional diver-profile field, and a course's `minimum_age` is enforced at booking **only when a date is on file**, measured on the day the course runs rather than the day it's booked. A diver with no date books exactly as before, so shipping it changed nothing for existing divers — option C (fail closed) was declined for exactly that blast radius, and stays available as a later upgrade once shops have backfilled. See [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md). **Depth ceilings: decided 2026-07-30 (Aaron Buxbaum), shipped the same day** ([20260730-site-depth-and-diver-age-surfaces](../architecture/decisions/20260730-site-depth-and-diver-age-surfaces.md)). `dive_sites` gets a structured numeric depth field (replacing/supplementing the free-text `depth_range`). When a site's depth exceeds what a diver's certification level supports (including the Junior 12 m/18 m/21 m bands), staff see a **warning, not a block** — an instructor may deliberately keep a diver shallower than the site's max, so the system flags rather than refuses. As built: `dive_sites.max_depth_meters` (canonical metres) versus `src/lib/depth-ceiling.ts`'s ceilings, surfaced as `depthAdvisory` — a sibling of readiness that can never become a blocker — over the deepest site the trip visits. A `shops.depth_unit` setting lets a shop read metres or feet without changing what is stored. **Medical-exception process:** covered by the existing waiver medical-questionnaire physician-referral gate (H-01–H-03 territory); no separate course-level exception process needed. |
| H-09 | In progress | Product owner + communications owner | Which channel sends booking and waiver notifications, and what consent, copy, timing, and sender identity apply? | Provider: Resend. The implemented baseline is immediate transactional booking confirmation, staff-triggered waiver link, and — new — the wait-list freed-seat invite, all through the one `notify()` seam; `RESEND_FROM_EMAIL` is the verified sender. Each of these sends for real when Resend is configured and degrades to a copyable/mailto composer or a hand-off link when it isn't. The owner dashboard shows the latest failed or unconfigured send. **The remaining channel and cadence scope is now implemented:** courtesy SMS via an AWS SNS `notifySms()` seam ([20260802-sns-sms-adapter](../architecture/decisions/20260802-sns-sms-adapter.md), superseding the original Twilio-backed design) and scheduled pre-trip reminder cadences (7-day and 24-hour) sent by an idempotent cron endpoint ([20260721-scheduled-reminder-cadence](../architecture/decisions/20260721-scheduled-reminder-cadence.md)), both degrading to `not_configured` until their provider env is set. Still open (policy, not mechanism): opt-in/consent policy, sender ownership, approved copy, the `CRON_SECRET` and `SNS_AWS_*` credential owners, and per-channel delivery monitoring/retries. **Policy decided 2026-07-24 (Aaron Buxbaum):** sender identity is the **shop's own name/brand** (already how every template greets — `src/lib/notifications/email.ts`); the existing transactional templates (booking confirmation, waiver link, freed-seat invite, 7-day/24-hour reminders) are **approved as the launch copy** — no new template library needed, the H-09 mechanism already shipped them. **Email is transactional/service messaging** (booking, waiver, trip logistics), so no marketing opt-in is required to send it. Credential owners for `RESEND_FROM_EMAIL`, `CRON_SECRET`, and `SNS_AWS_*` are **Aaron Buxbaum** (sole operator; same as H-04). Launch-minimum delivery monitoring is the existing owner-dashboard "latest failed/unconfigured send"; per-channel retries stay a later enhancement. **Still gated on the attorney: SMS opt-in consent** (TCPA and equivalents) — excluded from this decision; keep the SNS channel behind its unset env until that clears. | Durable notification policy and multi-channel delivery. |
| H-10 | Dropped | Product owner + operations lead | ~~Request supported C-card verification access from PADI, SSI, and NAUI, then choose the authorized verification source for each agency.~~ | — | Dropped: no agency exposes a usable C-card verification API, so the automated seam was removed in favour of manual staff certification — staff look the number up with the agency and mark the card certified. See [20260721-manual-certification](../architecture/decisions/20260721-manual-certification.md). |
| H-11 | Implemented | Dive operations lead + gas-blending authority | Which nitrox fill-station procedure, ppO₂ ceilings, mix band, O₂-clean tank tracking, and blender qualifications apply? | Approved fill-log of record, accepted ppO₂ limits, EANx band, and any per-agency card-acceptance rules. | **Approved as-is 2026-07-30 (Aaron Buxbaum):** the [provisional dive parameters](#nitrox-fills) — 22–40% O₂ mix band, MOD at ppO₂ 1.4 bar working / 1.6 bar contingency, fill logged only against a verified nitrox specialty card — are accepted as the operating policy, not just a provisional default. Still gated on **V-05** (`dive-domain-expert` sign-off) before a real fill station operates; per-agency EANx card-acceptance nuances, O₂-clean tank tracking, and blender qualifications weren't separately itemized in this approval and can be revisited if V-05 surfaces a gap. |
| H-12 | Chosen | Product owner + finance owner | Is the public founding-shop price, commercial term, support promise, and multi-location policy correct? | Approved monthly price, billing cadence, support/onboarding commitment, taxes/fees policy, and public contact/contract flow. | **Closed 2026-07-24**, per the [rollout plan's recommendation](rollout.md#04-commercial-decisions-h-12-h-13): the founding-shop price stays **$99 flat per location / month**, everything included, no setup fee, no per-seat math, no platform fee, cancel anytime with the export button — matching the figure already published from `src/lib/marketing.ts` (the source of truth) via the [pricing boundary](marketing.md#claims-policy-hard-rules), so no pricing-constant change was needed. Added two commercial terms, both now published as founding-shop claims on the pricing and home pages (`earlyAccessPrice` in `src/lib/marketing.ts`, `src/app/pricing/page.tsx`, `src/app/page.tsx`): the price is **locked for two years for the founding cohort**, and support is **founder-direct with same-day response**, for the founding cohort. Still open, and not blocking founding-cohort launch: standard (post-founding-cohort) pricing once the cohort closes, taxes/fees policy, and the precise public contact/contract intake flow. **Legal/tax review still required (2026-07-24, retained):** taxes/fees treatment is jurisdiction-dependent and the public contract/intake flow, the published **two-year price lock**, and the **same-day support** promise are binding commercial commitments — closing the *price* did not clear them. Have a qualified legal/tax reviewer sign these off before a shop is contractually held to them; the "finance owner" here means that reviewer, not a product-owner-only sign-off. **Cadence + support decided 2026-07-24 (Aaron Buxbaum):** billing is **month-to-month, per location, cancel anytime** (no setup fee, no annual contract) — now stated plainly in a pricing FAQ (`src/app/pricing/page.tsx`); the founder-direct same-day support commitment is already published. These plain terms are the owner's to set and match existing claims; the jurisdiction-dependent taxes/fees treatment, the binding two-year price lock, and the contract/intake flow remain gated on the legal/tax reviewer above. |
| H-13 | Implemented | Product owner + `dive-domain-expert` reviewer | CR-008 made `(shop_id, lower(email))` a hard uniqueness constraint for active people and, per the existing pattern, self-service paths (booking, wait-list, import) silently *reuse* the matching person on any email match — the submitted name is never compared. A `dive-domain-expert` review of that diff flagged this as **unsafe**: a shared-inbox submission (a spouse, or a minor booked under a parent's email — see the glossary's Junior-certification rules) can silently attach a new diver's booking to an existing person's verified cert/current waiver, skipping medical-questionnaire collection and cert verification for someone who never provided either. Decide whether email-only reuse is acceptable as-is, or requires a name-mismatch safeguard (e.g. route a mismatched name to a staff-verify state instead of auto-"ready", surface `findOrCreatePerson`'s `created: false` to staff, or a light "is this you?" confirmation) before a live shop relies on it. The same review separately flagged the soft-delete-frees-the-email window: if staff soft-delete the wrong person and a genuinely *different* new person claims the freed email before the mistake is caught, that new person gets a blank record with no link back — bounded (fails closed, no data ever becomes ambiguous) but worth a deliberate policy call rather than leaving implicit. | Accepted behavior or a chosen safeguard design, and whether the soft-delete window needs a mitigation before production. | **Decided + shipped 2026-07-24: add the name-mismatch safeguard; accept the soft-delete window as-is.** `findOrCreatePerson` now returns `nameMatches` (a case/accent/order/middle-initial-tolerant compare, `src/lib/person-name.ts`), and a public booking that reuses an email under a genuinely different name is stamped `bookings.identity_unconfirmed_at`. That raises a fail-closed `identity_unconfirmed` readiness blocker — surfaced on the roster, Today queue, and manifest — so a shared-inbox booking can't board on the matched diver's certs/waiver until staff tap **Confirm identity**. Reuse itself is unchanged (the person is never forked); only *readiness* is withheld. The soft-delete-frees-the-email window was accepted as-is (already fails closed to a blank record). Recorded in the [identity match key glossary entry](glossary.md#modeling-notes) and [20260723-person-email-uniqueness ADR](../architecture/decisions/20260723-person-email-uniqueness.md). **Still required before merge:** `security-reviewer` + `dive-domain-expert` sign-off per AGENTS.md (safety-critical + personal-data change). |
| H-14 | Implemented | Product owner | The [codebase review](../architecture/decisions/20260724-staff-session-and-capability-migration-policy.md#context)'s "Human-owned decisions" item 1: current ADRs let every staff role reach nearly every staff surface, while product language implies owners/managers configure money and crew/captains run operations. Does DiveDay need real role boundaries on payment settings, refunds, waiver templates, diver deletion, and trip configuration? | **Decided 2026-07-24: yes**, and **implemented 2026-07-24**. Five predicates in `src/lib/authz.ts` (`canManagePaymentSettings`, `canRefund`, `canManageWaiverTemplates`, `canDeleteDiver` → owner/manager; `canConfigureTrips` → owner/manager/instructor), with live DB-checked companions in `src/db/authz.ts` (`loadActiveStaffRoles` + `canPersonX`) so a demoted/disabled/deleted staff member loses the surface immediately (closing the H-15 window for money, legal text, and roster deletion). Enforced in both layers per ADR-0006 — each surface's page hides the control **and** its server action(s)/route re-check, refunds gated in `removeBookingAction`/`refundPaymentAction`/order `refundAction`, trip config across `trips/[id]` and `trips/new`. See [20260724-role-authorization](../architecture/decisions/20260724-role-authorization.md). | Done — see the ADR and `src/db/authz.test.ts` / `e2e/role-permissions.spec.ts`. `security-reviewer` + `dive-domain-expert` review before merge per AGENTS.md. |
| H-15 | Implemented | Product owner | The [codebase review](../architecture/decisions/20260724-staff-session-and-capability-migration-policy.md#context)'s "Human-owned decisions" item 2: most staff mutations trust role-bearing JWT state until the next sign-in. What is the tolerated disable/demotion delay, and is a shared `requireActiveStaffSession` database recheck needed? | **Decided 2026-07-24: accept the existing default, no change.** `src/lib/auth.config.ts` already uses Auth.js's own default session lifetime (30-day `maxAge`, 1-day `updateAge`) with no override — a disabled/demoted staff member's session can keep stale roles for up to 30 days. The product owner confirmed this is an acceptable, non-aggressive tolerance; no `requireActiveStaffSession` recheck was added. | None — no code change. Revisit only if a specific incident or compliance requirement narrows the acceptable window. See [20260724-staff-session-and-capability-migration-policy](../architecture/decisions/20260724-staff-session-and-capability-migration-policy.md). |
| H-16 | Implemented | Product owner | The [codebase review](../architecture/decisions/20260724-staff-session-and-capability-migration-policy.md#context)'s "Human-owned decisions" item 3: CR-001–CR-003 replaced permanent readiness/confirmation authority with revocable capabilities — was invalidating historical URLs a one-time cutover or an ongoing/permanent migration policy? | **Decided 2026-07-24: it was already a one-time cutover, and it already happened.** Shipping `booking_capabilities` deleted the old `src/lib/readiness-links.ts` HMAC module outright rather than keeping it as a fallback, so every pre-cutover link has had no verification path since that PR merged — nothing further to migrate. There is no standing "migration policy" governing future capability changes; each would be its own decision. | None — already complete. See [20260724-staff-session-and-capability-migration-policy](../architecture/decisions/20260724-staff-session-and-capability-migration-policy.md). |
| H-17 | Implemented | Product owner (sanity-checked by the generalist attorney — Tier 1, see [legal-engagement-scope.md](stakeholders/legal-engagement-scope.md)) | Should the contact importer trust a prior shop's own waiver acceptance — including its medical clearance — well ahead of the still-open H-01–H-03 legal sign-off on DiveDay's own waiver policy, and without requiring a per-row staff attestation the way the existing paper-signature path does? The assistant building this flagged the conflict with the contact importer's documented fail-closed medical rule ([20260723-contact-importer](../architecture/decisions/20260723-contact-importer.md)) and recommended a staff-attested, medical-still-fail-closed alternative before implementing. | **Decided 2026-07-24: yes, trust it — including medical clearance — marked distinctly as "imported," with no per-row staff attestation required.** The product owner considered and explicitly declined both of the assistant's safer alternatives (staff attestation; medical-only fail-closed). This decides only what a *contact import* does; it does not resolve H-01–H-03, which still gate DiveDay's own waiver policy before any shop's use of either path is production-ready. | Implemented: `signatureMethod: "imported"` waiver records, exempted from the template-version currency check but not the one-year age check. See [20260724-import-waiver-acceptance](../architecture/decisions/20260724-import-waiver-acceptance.md). |
| H-18 | Ready | Product owner + generalist attorney + CPA (Tier 1 — no specialist needed, see [legal-engagement-scope.md](stakeholders/legal-engagement-scope.md)) | Corporate footing for DiveDay: operate inside the owner's existing **Pseudorandom LLC** (S-corp election; current business is software contracting and mentorship — owner context recorded 2026-07-24) or form a dedicated DiveDay entity — plus the SaaS contract set nothing can be signed without: design-partner pilot agreement, founding-shop subscription terms encoding the published two-year price lock and same-day support promises (H-12), a privacy policy, and the "your waiver is your responsibility" boundary. The working recommendation is a dedicated LLC before any pilot agreement is signed; analysis and the generalist/CPA question list are in [stakeholders/legal.md](stakeholders/legal.md#the-entity-decision-h-18). | Chosen entity and structure (sibling vs. subsidiary), formation/foreign-qualification state(s), interim signing posture if pilots start first, and the executed contract templates. | Blocks pilot agreements, insurance binding (H-19), the Stripe platform application details (H-07), and the SMS carrier compliance registration (H-09). |
| H-19 | Deferred | Product owner + commercial insurance broker | Does DiveDay itself carry technology E&O and cyber coverage — it stores medical flags and waiver evidence for a safety-adjacent activity — before a pilot shop's real data is live, and at what limits? Needs the H-18 entity direction first (the policy names the insured). Playbook: [stakeholders/insurance.md](stakeholders/insurance.md). | Bound policies with carrier, limits, and effective date — or a written, deliberate decision to proceed uninsured. | **Decided 2026-07-30 (Aaron Buxbaum): go without coverage until a pilot is closer to real**, then revisit with a broker (needs the H-18 entity direction first, since the policy names the insured). Deliberately not a permanent close — row stays gating on binding an actual policy before a pilot shop's real data goes live. Pilot agreements can reference coverage; distinct from the shop-side "what your insurer will ask" sales asset, which has no gate row. |
| H-20 | Implemented | Product owner | Should the contact importer trust what a shop already had in its own system — landing imported certification and nitrox cards as `verified` rather than as unverified claims re-checked at first contact — on the reasoning that a record already in a system was manually verified there? And should this apply to all sources, including a loose spreadsheet? The assistant flagged the sharp edge (an imported level card clears depth gates, and an imported nitrox card authorizes enriched-air fills, on the strength of a CSV cell) and offered "keep it gated" and "verified, no confirm" alternatives before implementing. | **Decided 2026-07-24: land imported cards `verified`, permanently flagged `imported`, surfaced for a soft one-tap staff confirm — for all sources including spreadsheets.** Boarding and depth clear on import; card expiry still applies, no card imports without a real number, and medical answers are never reconstructed. **The one exception, on `dive-domain-expert` recommendation (owner confirmed 2026-07-24):** the enriched-air *fill* waits for the confirm — an imported nitrox card gives plain air until a staffer taps confirm, because a nitrox card has no expiry backstop and a wrong fill is the highest-consequence failure. This reverses [20260723-contact-importer](../architecture/decisions/20260723-contact-importer.md)'s "claimed, never verified" rule; it does not resolve the still-open waiver legal gates (H-01–H-03). Also authorized in the same decision: extend the free concierge switch offer to **all** switching pages and make it bidirectional (help a shop switch on *and* off DiveDay). | Implemented: `importedAt`/`importedFromLabel` on `certifications`/`nitrox_certifications`, verified-on-import write path, inline Confirm-card affordance + roster "to confirm" count, PDF import documents, and the rewritten `IMPORT_HONESTY_TABLE`. See [20260724-import-verified-cards](../architecture/decisions/20260724-import-verified-cards.md). |
| H-21 | Implemented | Product owner + dive operations lead | H-08's minimum-age gate (`people.date_of_birth`, [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md)) made age a piece of data DiveDay now holds and enforces, but it surfaces nowhere a crew actually looks: the roster, the manifest, and roll call show no age or minor indicator, so a captain reading the boarding list has no way to know a booked diver is 12 without opening their profile. Separately, and only reachable now that the concept of a minor exists in the product at all: a minor can sign their own liability waiver through the ordinary `/waivers/[token]` flow — there is no guardian-signature step, no co-signer field, and no block on a minor completing it solo. Does the roster/manifest need an age or minor indicator, and does the waiver flow need a guardian-signature path for a diver under a jurisdiction's age of majority (or under the agency's junior-certification threshold, 15)? | Whether to surface age/minor status on staff-facing rosters and manifests (and where), and whether/how the waiver flow requires a parent/guardian signature for a minor — a design (co-signer capture, a distinct minor waiver template, a staff-witnessed paper fallback) or a deliberate deferral with the current solo-signature behavior accepted as-is pending H-01–H-03's broader waiver legal review. | **Decided 2026-07-30 (Aaron Buxbaum):** the roster surfaces age in years (when a date of birth is on file) and a minor indicator. Also authorized in the same decision: a celebratory birthday callout — a section similar to the existing dive-count milestone (e.g. "25 dives") that shows when a diver's birthday is today or coming up soon. **Solo-signature is accepted as-is for now** — explicitly called out (not silently left open) so it comes back for review alongside the broader H-01–H-03 waiver legal work rather than being forgotten. **Shipped 2026-07-30** ([20260730-site-depth-and-diver-age-surfaces](../architecture/decisions/20260730-site-depth-and-diver-age-surfaces.md)): the roster and the boat manifest carry age in years, a **Minor** badge (18 — legal majority, the guardian-signature threshold; the diving world's 15 is handled separately by the junior depth bands), and a birthday badge, with a `Celebrations` section above the roster. The owner set the window at **seven days in either direction** and the copy at icon-plus-timing only ("🎂 in 2d" / "🎂 2d ago"). The "existing dive-count milestone" this row referred to does not exist in the codebase, so the callout's shape was confirmed with the owner rather than guessed. Solo minor signature remains open and stays flagged for H-01–H-03 — the badge makes it visible to staff, it does not close it. |
| H-22 | Implemented | Product owner | H-08's minimum-age gate as approved (2026-07-24, option B — "collect it, fail open") did not specify how the anonymous public booking form should behave, and a `security-reviewer` pass on the implementation ([20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md)) found that refusing a public booking on age is an exploitable oracle: it lets anyone who can guess a diver's email learn whether that email belongs to a child under a course's minimum age (10/12/15/18), probing for free since the check ran ahead of the capacity check. The fix that shipped goes further than "fail open on a missing date" — it never refuses the public form on age at all (even with a date on file and a real underage match), relies entirely on the `under_minimum_age` readiness blocker to catch it downstream, and gives that blocker copy worded identically to the identity-mismatch blocker so its presence discloses nothing on the diver's own checklist either. Was this narrower scope — publicly silent on age, full disclosure withheld even from the diver-facing checklist — the intended shape of "option B," or should the public path refuse more visibly once identity is confirmed (e.g. after `identityUnconfirmedAt` clears), accepting some reduced oracle risk for a clearer diver-facing signal? | Confirm the shipped behavior (silent public path, generic diver-facing copy, staff see the real reason) as final, or specify a different disclosure boundary once a diver's identity is confirmed. | No code currently blocked — this is a retroactive confirmation of a security-motivated implementation choice, not a gate on new work. A different answer would be a follow-up change to `src/db/bookings.ts` and `src/lib/readiness-summary.ts`. **Decided + shipped 2026-07-25: name the real reason on the diver's own checklist.** Before choosing, the assistant flagged that the example gate this row itself suggested — "once `identityUnconfirmedAt` clears" — doesn't actually work: that flag only fires on a *mismatched* submitted name (H-13), so an attacker who already knows a specific child's real name and email gets `nameMatches: true` on the first request and never trips it, meaning that gate would disclose immediately to exactly the more-targeted attacker it needed to stop. **The product owner chose to proceed with that gate anyway** — the common non-adversarial case (a family whose child isn't old enough) now gets an actionable message — with the known gap written down rather than silently shipped. `DIVER_VOICE.under_minimum_age` (`src/lib/readiness-summary.ts`) now names the real reason whenever no identity mismatch is also present; the mismatched-name case still gets the fully generic line. See [20260725-checklist-age-disclosure](../architecture/decisions/20260725-checklist-age-disclosure.md) for the full tradeoff and the stated escape hatch (a real identity-proof primitive, if one is ever built). |
| H-23 | Implemented | Product owner | The owner asked to move rows from the published honesty table's "Stays behind" column into "Comes across" across every switching surface. Specialty cards (deep, wreck, night, drysuit) were the one row that was a capability gap rather than a boundary we intend to keep — and importing them removes a mitigation [20260724-import-verified-cards](../architecture/decisions/20260724-import-verified-cards.md) relied on, since "specialties stay gated" was part of why an imported ladder card was allowed to clear the boarding/depth gate. A specialty authorizes a materially riskier dive (deep gates depth past 18 m), so the assistant put three postures to the owner before building: clear the gate on import like a ladder card, land verified but hold the gate until the one-tap staff confirm, or import `pending` and re-verify everything. | **Decided 2026-07-25: specialty cards import `verified` and flagged imported, and the specialty gate holds until a staffer taps confirm.** Boarding is never what waits — only the dive that requires the specialty. One card number is never stretched to two cards: a cell naming two specialties imports neither. Payment methods and booking/service history stay behind by design (processor boundary; needs a full-shop importer). Same decision: card expiry now travels with an imported card (including a past date, which lands as expired) and dive insurance comes across as free text. | Implemented: `imported_at`/`imported_from_label` on `specialty_certifications`, the `specialty_import_unconfirmed` readiness blocker, `certification_expires_at` + `dive_insurance` import fields, and the rewritten honesty-table rows. See [20260725-import-specialty-cards](../architecture/decisions/20260725-import-specialty-cards.md). |
| H-24 | Implemented | Product owner | Two findings the `dive-domain-expert` review raised against the specialty-card import (H-23) and which that change deliberately deferred: (1) the one-tap **Confirm card** asserted nothing — it stamped `reviewedAt` with no prompt and no claim, even though for an imported specialty card that tap is what opens a depth gate, and for an imported nitrox card what authorizes an enriched-air fill; and (2) `normalizeLevel` read anything containing "advanced" as Advanced Open Water, so **TDI Advanced Nitrox** — a decompression-adjacent gas certification — imported as a *verified* AOW card two rungs above Open Water, with nothing holding it (a ladder card clears its gate on status alone). | **Decided 2026-07-25: fix both.** Confirming an *imported* specialty or nitrox card now requires an explicit card-sighting attestation ("I've seen this diver's card, or checked the number with the issuing agency"), recorded in the card's review note; a card the shop captured itself is unaffected, and there is deliberately no bulk confirm. A technical or overhead-environment rating (trimix, CCR, cave, deco, tec, Advanced Nitrox…) imports as **nothing**, named in the preview as a technical rating that wasn't imported — the owner's instruction was "fix both, or just show that it didn't import and why". | Implemented: `cardSighted` on both review paths with a `card_sighting_required` refusal, the attestation control on the diver record, `TECHNICAL_CERT`/`DISCIPLINE_QUALIFIER` in the importer. See [20260725-imported-card-sighting](../architecture/decisions/20260725-imported-card-sighting.md). |

## Human verification queue

| ID | Status | Human owner | Work to perform | Evidence of completion |
| --- | --- | --- | --- | --- |
| V-01 | Ready | Product owner or QA owner | Browser-check the merged M5/M6 experience in light and dark mode on a desktop viewport and a phone viewport: pack and return gear, service and retire eligible gear, view a manifest with blockers, board an eligible diver, reject a blocked diver, and print/save the manifest. | Browser/OS, viewports, test data, result, defects, and screenshots or a short screen recording. |
| V-02 | Ready | Dive operations lead + `dive-domain-expert` reviewer | Field-test the manifest on a phone outdoors with realistic marina connectivity. Include glare/wet hands, automatic save + airplane-mode reload, a blocked diver, before-departure and after-dive roll calls, a deliberately newer live event conflict, reconnection, an expired/undecryptable device copy recovering on its own (no delete button exists), and print/PDF fallback. | Date, device/browser, network conditions, scenarios, freshness shown, reconciliation results, findings, screenshots/video, reviewer sign-off, and whether production departures may proceed. |
| V-03 | Ready before production | Product owner + qualified legal reviewer | Review the configured waiver/medical flow against the approved H-01–H-03 policies and confirm staff know how to handle a medical-review blocker. | Signed-off policy version, staff training owner/date, and escalation contact. |
| V-04 | Ready before production | Operations lead | Load real initial inventory, staff roles, trips, and pilot bookings; then rehearse check-in, packing, return, and roll call. | Pilot checklist, discrepancies found, and any required data cleanup. |
| V-06 | Ready before a Spanish-language shop goes live | Product owner + a native Spanish speaker with dive-industry vocabulary | Review the `es-ES` diver-facing bundle (`src/i18n/locales/es-ES/diver.json`) for accuracy, register, and dive-industry terminology before the language is offered to a real shop. It is a first-pass translation written alongside the feature, not a professional one. Pay particular attention to the booking and readiness copy, where a mistranslation changes what a diver believes they have agreed to. Note the deliberate scope boundary: the waiver body, the medical questionnaire, and `/ready` are **not** translated and stay English pending H-01/H-03, so a Spanish shop currently shows a visible language seam at the waiver. | Reviewer name and date, corrections applied, and a decision on whether the seam at the waiver is acceptable for a pilot or whether H-01/H-03 must clear first. See [diver-copy-localization](../architecture/decisions/20260729-diver-copy-localization.md). |
| V-05 | Ready before production | `dive-domain-expert` reviewer | Review the nitrox fill slice for domain correctness: EANx mix band, MOD formula and ppO₂ ceilings, the verified-card gate, and analysis-signature evidence. | Reviewer sign-off, any corrections to the [provisional parameters](#nitrox-fills), and confirmation the write-time gate matches shop policy. |

## Existing implementation boundaries

- **Waiver evidence:** the first release keeps immutable local records with typed consent; it does
  not claim cryptographic non-repudiation. The signature *provider* is settled — local for now
  (H-03, 2026-07-24) — but whether typed consent is a legally sufficient assurance level is still
  open, so the unresolved policy work is H-01 through H-03. A contact-imported record
  (`signatureMethod: "imported"`, H-17) trusts a prior shop's own acceptance instead of local
  consent capture — a distinct, separately decided evidence source; see
  [20260724-import-waiver-acceptance](../architecture/decisions/20260724-import-waiver-acceptance.md).
- **Manifest:** the live source remains derived and append-only. Offline use is an explicitly saved,
  encrypted device snapshot; it can disappear with browser storage, does not update across devices,
  and displays stale readiness rather than presenting it as live. Print/PDF is the independent
  fallback. Production operation remains blocked on H-05 policy approval and V-02 field evidence.
- **Provider choices:** payment, notification, signature, and similar integrations must remain
  behind a small provider seam rather than spreading vendor SDK calls through the application. See
  the provider-seam rule in [architecture/overview.md](../architecture/overview.md#cross-cutting-rules).
- **Rental requests:** the first request is an editable planning input, never an inventory
  reservation or fit/weight authorization. Staff allocation remains the conflict-safe source of
  truth.

## Provisional implementation defaults — verify before production

These are practical starting points used by the first course, gear, nitrox, and hosting slices.
They are not legal, agency, medical, or operations policy — the decision register above remains the
source of approval work; each row here maps to the H-row that must sign it off.

### Waiver and signature

- **Starting form shape:** liability release / assumption of risk / non-agency acknowledgement plus
  a medical questionnaire. This follows the structure of PADI's commonly encountered digital form
  set, not copied PADI text. The shop must use approved, jurisdiction-appropriate language before
  it sends a real waiver.
- **Starting signature:** typed full name, explicit agreement, timestamp, immutable template
  snapshot, and an expiring private completion link. This is a convenient electronic-consent
  baseline, not a claim of cryptographic non-repudiation or a substitute for legal advice.
- **Must verify (H-01–H-03):** jurisdiction, approved template and medical questions, age/guardian
  rules, retention/deletion, and privacy notice. The signature-*provider* question (H-03) is
  decided — the local typed-consent provider is kept for now — but whether typed consent is legally
  *sufficient* (H-03's assurance gate) is still open and depends on this legal review.

Sources: [PADI digital forms](https://pros-blog.padi.com/digital-forms-expand/),
[PADI general-training release](https://pro-cms.padi.com/sites/default/files/documents/training-hub/10072_Liability_Release_v403_FF_EN.pdf),
and [PADI diver medical questionnaire](https://www.padi.com/sites/default/files/documents/2020-08/10346E_Diver_Medical_Form.pdf).

### Course admission

- **Starting rules:** Discover Scuba Diving and Open Water have no pre-existing C-card gate;
  Advanced Open Water and a refresher require a verified Open Water card. Instructor-led sessions
  cannot accept a booking until an instructor is assigned.
- **In-water ratio (resolved + shipped):** an ungated (DSD/Open Water) session additionally caps
  bookings at PADI's published entry-level ratio — 8 students per instructor, +2 per certified
  assistant, ceiling 12 — independent of the trip's own stated capacity. See
  [20260724-course-admission-standards](../architecture/decisions/20260724-course-admission-standards.md).
- **Minimum age (resolved + shipped):** an optional diver date of birth gates a course's stated
  `minimum_age`, measured on the day the course runs. Fails open — no date on file, no block
  (H-08 option B). See
  [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md).
- **Depth ceilings (including the Junior age bands): resolved + shipped 2026-07-30.**
  `dive_sites.max_depth_meters` is compared against the diver's certification ceiling and Junior
  age band (12 m/18 m/21 m); a deeper site shows staff a **warning, not a block** — an instructor
  may deliberately keep a diver shallower than the site max. Ceilings are held as published
  metre/foot pairs and compared in the shop's own unit, so a 60 ft site does not read as
  over-limit for a 60 ft card. See
  [20260730-site-depth-and-diver-age-surfaces](../architecture/decisions/20260730-site-depth-and-diver-age-surfaces.md).
- **Still open:** local regulatory/insurer rules per course/environment, and the **junior
  supervision** half of the Junior rule (10–11 with a professional or certified guardian; 12–14
  with any certified adult) — documented in the glossary, deliberately not modeled, so DiveDay
  says nothing about buddy pairing for a junior diver.

### Rental gear request

- **Starting rental set:** BCD, regulator, wetsuit, mask/fins, weights, dive computer, and tank.
  Per the H-06 decision of 2026-07-24 the **dive computer defaults on for every diver** and is in
  the default catalog, but is **priced on its own line, not folded into the "set" bundle** (the set
  stays the five hard-goods pieces); the GoPro is the one default-off add-on. The request asks for
  BCD/wetsuit size, boot/fin size, usual weighting, and notes.
- **Safety boundary:** a request is not a reservation or fit approval. Staff still assigns a real,
  available item and confirms fit/weight at check-in.
- **Must verify (H-06):** shop inventory packages, thickness/temperature guidance, measurement
  method, and tank policy. (Computer policy is decided: default-on for every diver, priced
  separately. Substitution authority and the unavailable-size fallback are decided *and built* —
  `canOverrideGearRequest` and the needs-staff-fit state, see
  [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md).)

Source: [example dive-rental reservation form with package and size fields](https://www.sailcaribbeandivers.com/wp-content/uploads/2024/10/SCD-RENTAL-FORM-2024-25.pdf).

### Booking checkout

- **Starting policy:** the full per-diver price (trip price, or the course's priced pair) is asked
  for at booking through one hosted Stripe Checkout on the shop's connected account; no tax lines,
  no platform fee, no automated refund. Refunds stay staff-initiated from the diver's payment
  context.
- **Deposit (opt-in, shipped):** a shop may set an optional per-diver `deposit_cents` on a trip. It
  charges that at booking (labelled a deposit on the Stripe page), settles the booking to
  `deposit_paid`, and shows the diver the balance still due at the dock. Off by default (null =
  full fare); a deposit at or above the price is ignored and charges full. DiveDay ships no default
  deposit amount — the value is the shop's commercial term. See
  [20260721-deposit-cancellation-policy](../architecture/decisions/20260721-deposit-cancellation-policy.md).
- **Cancellation window (opt-in, shipped):** a shop may set `cancellation_window_hours` (hours
  before departure a diver can cancel for a refund). It is *shown* to divers at booking and on the
  confirmation, and to staff as a "refund-eligible until" cue on paid seats. Off by default (null =
  no stated window); DiveDay ships no default window.
- **Automated refund inside the window (shipped):** cancelling a paid booking inside a stated window
  now refunds the Stripe payment automatically through the shop's own connected account and settles
  the booking to `refunded`. It moves money only on a confirmed Stripe reversal; a counter/cash
  payment, a disconnected account, a past-deadline (forfeit) cancel, or a Stripe failure all degrade
  to the staff-run refund, surfaced in the trip notice. No stated window = no automation. See
  [20260721-automated-cancellation-refund](../architecture/decisions/20260721-automated-cancellation-refund.md).
- **Safety boundary:** payment never gates the capacity-safe booking transaction — a diver who
  abandons checkout keeps the seat as an ordinary unpaid booking, surfaced by the existing
  `payment_due` blocker where the trip requires payment. Paid/deposit state is recorded only from
  Stripe's own webhook or API responses, never from a return URL.
- **Opt-out:** leave the trip unpriced or the Stripe account unconnected and the public flow stays
  book-now-pay-later.
- **Must verify (H-07):** the deposit-amount and cancellation-window *values* a shop should be
  guided toward (DiveDay ships none), percentage vs. flat deposits (mechanism deferred by owner
  request), tax treatment, any platform fee, and whether unpaid bookings should auto-expire.
  Automated refunds inside the window are now implemented; the remaining refund question is only
  whether a partial-refund tier is ever wanted.

### Nitrox fills

- **Starting mix band:** whole-percent recreational EANx from 22% to 40% oxygen. Below 22% is
  treated as air; above 40% is a technical mix outside this slice. Non-integer and out-of-band
  values are rejected rather than logged.
- **Starting MOD basis:** maximum operating depth is derived as `10·(ppO₂/FO₂ − 1)` metres, floored,
  at a default working ppO₂ of **1.4 bar** with a **1.6 bar** contingency option. The value is
  computed from the analyzed mix, never entered by hand.
- **Starting gate + evidence:** a fill is only logged for a diver with a **verified** nitrox
  specialty card, and it records the diver's typed analysis signature, the mix, the ppO₂ ceiling,
  and the deriving staff member. It does not replace the diver's own pre-dive O₂ analysis.
- **Must verify (H-11, V-05):** agency/blending-facility fill-station procedure, whether a signed
  analysis sticker or fill log of record is required, the accepted ppO₂ ceilings for the shop's
  diving, gas-blender qualifications, O₂-clean tank tracking, and any per-agency EANx card
  acceptance rules.

Sources: [DAN — enriched air nitrox and ppO₂/MOD limits](https://dan.org/alert-diver/article/the-basics-of-nitrox/),
[NOAA Diving Manual oxygen exposure limits](https://www.noaa.gov/).

### Vercel hosting

Vercel is the selected web host and Neon (through Vercel's Marketplace integration) is the
production Postgres provider. The Vercel production build applies committed Drizzle migrations
before building the application; preview builds intentionally skip them. Vercel System Environment
Variables are enabled, so the build can identify production through `VERCEL_ENV`. Backups, domain,
secrets, and incident ownership still need H-04 completion. See the [Neon hosting
ADR](../architecture/decisions/20260718-vercel-neon-hosting.md).

## Change history

| Date | Change | Owner |
| --- | --- | --- |
| 2026-07-18 | Created from the remaining product and operational work after M5 gear and M6 live manifest. | Product team |
| 2026-07-18 | Selected Vercel hosting; added provisional waiver/signature/course/gear baselines for human review. | Product team |
| 2026-07-18 | Implemented Neon as the H-04 Postgres provider (node-postgres driver, `pnpm db:migrate` runbook); owner/backup/incident-response naming still open. | Engineering |
| 2026-07-18 | Configured Vercel production builds to run committed Drizzle migrations before building the app; preview builds remain read-only. | Engineering |
| 2026-07-18 | Shipped the M7 nitrox fill-log slice; added H-11 fill-station policy and V-05 dive-domain review. | Product team |
| 2026-07-18 | Added public homepage, product, and pricing surfaces with real-demo screenshot capture; recorded H-12 for commercial approval. | Product team |
| 2026-07-18 | Researched PADI, SSI, and NAUI verification access; added agency-specific gateway configuration and the credential setup runbook. | Engineering |
| 2026-07-18 | Connected a private Vercel Blob store for certification-card uploads and configured its token in Vercel; updated the card-image ADR and roadmap. | Engineering |
| 2026-07-18 | Enabled Vercel System Environment Variables so production builds can identify `VERCEL_ENV` and run the documented migration path. | Engineering |
| 2026-07-18 | Implemented the M6 offline snapshot/reconciliation policy and expanded V-02 into the required outdoor safety review. | Engineering |
| 2026-07-19 | Implemented Stripe Connect (Standard) so shops bring their own account, plus orders/invoices with webhook-confirmed payment; updated H-07 to reflect the settled provider/mechanism decision, with deposit/cancellation/refund/tax policy and a Connect platform-credential owner still open. | Engineering |
| 2026-07-21 | Implemented public checkout-at-booking on the Connect substrate (hosted Stripe Checkout after the capacity-safe booking, webhook + API-read confirmation); recorded the provisional full-price/no-deposit policy for H-07 review. | Engineering |
| 2026-07-21 | Shipped the H-07 deposit and declarative cancellation-window *mechanisms* (opt-in per-trip `deposit_cents` charging to `deposit_paid`; `cancellation_window_hours` shown to divers and staff, refunds still staff-run); off by default, no default values — the policy values remain H-07. Wired the H-09 wait-list freed-seat invite through the `notify()` seam so it sends for real by default, with the composer as fallback. | Engineering |
| 2026-07-21 | Owner chose to automate the H-07 refund and close the remaining H-09 channel/cadence scope. Shipped: automated refund inside a stated cancellation window (Stripe reversal on the shop's account, degrading to staff-run everywhere else); SMS/WhatsApp via a fetch-based Twilio `notifySms()` seam; and scheduled 7-day/24-hour pre-trip reminder cadences sent by an idempotent `GET /api/cron/reminders` endpoint (Vercel Cron, `CRON_SECRET`-guarded), with a courtesy SMS alongside email. Percentage deposits deferred by owner request. All off until their provider env is set. | Engineering |
| 2026-07-24 | Shipped the CR-013–CR-021 codebase-review follow-up tickets (PR #144) and archived the review to `product/archive/codebase-review-20260723.md`. Recorded its three "Human-owned decisions" as H-14 (role authority boundaries: **Chosen**, yes — not yet implemented), H-15 (staff-JWT revocation window: **Implemented** as the existing 30-day Auth.js default, no change), and H-16 (capability migration policy: **Implemented**, confirmed as an already-complete one-time cutover). See [20260724-staff-session-and-capability-migration-policy](../architecture/decisions/20260724-staff-session-and-capability-migration-policy.md). | Product owner + Engineering |
| 2026-07-24 | Recorded three product-owner decisions. **H-03:** keep the local signature provider for now — provider sub-decision only; the row stays **Ready** because whether typed consent is a sufficient assurance level is still open (H-01–H-03/V-03). **H-06:** the dive computer joins the default rental kit and the priced core "set" bundle — shipped in `src/lib/rentals.ts` with tests, glossary, and the rental-request note updated (arrived labelled "H-05"; belongs to the gear-policy row). **H-12:** the founding-shop price is approved at $99 per location/month for now, matching the published `src/lib/marketing.ts` figure; billing cadence, support/onboarding, taxes, and contract flow still open. | Product team |
| 2026-07-24 | Follow-up after `dive-domain-expert` + automated review of the H-06 change. The dive computer is now part of the **persisted** default catalog (`shops.rental_items` default single-sourced from `DEFAULT_SHOP_RENTAL_ITEMS`, with a backfill migration for existing shops on the old five-item default), so newly onboarded shops actually offer it. Made the "set" eligibility relative to the core items each shop offers (`quoteRentalFit`), so a shop that doesn't stock a computer still reaches its set, and the diver-facing set copy is derived from the offered core. Propagated the H-12 price approval into `marketing.md`, `rollout.md`, and `roadmap.md` (price-specific "provisional" statements only; billing/support/tax/contract items left open). | Engineering |
| 2026-07-24 | Implemented **H-14** (role authorization). Added five role predicates to `src/lib/authz.ts` and live DB-checked companions in `src/db/authz.ts` (`loadActiveStaffRoles` + `canPersonX`), gating payment settings, refunds, waiver templates, and diver deletion to owner/manager and trip configuration to owner/manager/instructor. Enforced in both layers per ADR-0006 — each surface's page hides the control and its server action(s)/route re-check against live roles — across the settings page + Stripe `connect` route, order/diver/trip-cancellation refunds, the waiver template + jurisdiction, diver delete/restore, and trip create/edit. New ADR [20260724-role-authorization](../architecture/decisions/20260724-role-authorization.md), unit tests (`src/db/authz.test.ts`), and a role-lens e2e (`e2e/role-permissions.spec.ts`). `dive-domain-expert` review narrowed the trip gate to trip *definition* (details, requirements, whole-series ops, creation, reinstate): the day-of operating actions the glossary assigns to crew — predicted-conditions entry, day-of crew assignment (manifest accuracy), and a single trip's weather cancellation — stay open to all staff. `security-reviewer` found no exploitable bypass and prompted defensive early-returns on the authz guards. | Engineering |
| 2026-07-24 | H-06 pricing refinement (owner request): the dive computer stays **default-on** and in the default catalog but is **priced on its own line, not folded into the "set" bundle** — reversing the earlier bundle choice so an own-computer diver keeps the full-set discount on the five hard-goods pieces. Removed the computer from `CORE_RENTAL_KINDS`, priced it per piece alongside the GoPro, and updated the unit/e2e tests, glossary, and rental-request note. | Engineering |
| 2026-07-24 | Owner requested the contact importer trust a prior shop's own waiver acceptance (medical clearance included), against the assistant's initial recommendation of a staff-attested, medical-fail-closed design; recorded as H-17 (**Implemented**), decided knowingly against the contact importer's original fail-closed medical rule. Also shipped export photo bundling: the full-shop export now includes real image files for every DiveDay-stored photo, not only URL references. See [20260724-import-waiver-acceptance](../architecture/decisions/20260724-import-waiver-acceptance.md) and [20260724-export-bundled-photos](../architecture/decisions/20260724-export-bundled-photos.md). | Product owner + Engineering |
| 2026-07-24 | **Closed H-12**, per the rollout plan's recommendation: kept the $99 flat per-location/month price (no pricing-constant change — already the published `src/lib/marketing.ts` figure) and added two founding-cohort commercial terms — a **two-year price lock** and **founder-direct, same-day-response support**. Published both as claims on the pricing page (card + FAQ) and the homepage closing band, sourced from `earlyAccessPrice` in `marketing.ts`. Propagated the closure into `marketing.md`'s claims policy and `roadmap.md`'s pricing-posture gate. Billing cadence, taxes/fees, and the public contract/contact flow remain open but non-blocking. | Product owner + Engineering |
| 2026-07-24 | Implemented two product-owner decisions. **H-08 (relabel half):** the certification/specialty "expiry" field is now presented to staff as a shop-set **"refresher due"** date (form label, diver-profile line, badge, and cert/specialty readiness blockers) — real C-cards don't expire; stored column, gating, readiness codes, and CSV `expires_at` unchanged. Glossary + certification-expiry ADR updated; course/agency rules stay open. **H-13:** shipped the name-mismatch identity safeguard — `findOrCreatePerson` returns `nameMatches` (`src/lib/person-name.ts`), a public booking reusing an email under a different name is stamped `bookings.identity_unconfirmed_at`, raising a fail-closed `identity_unconfirmed` readiness blocker cleared by a staff **Confirm identity** roster action; the soft-delete window is accepted as-is. Glossary + person-email-uniqueness ADR updated. `security-reviewer` + `dive-domain-expert` review still required before merge. | Product owner + Engineering |
| 2026-07-24 | Recorded three product-owner decisions and retained a legal caveat. **H-04:** production region is Washington, D.C. / US-East (Neon `aws-us-east-1`); **Aaron Buxbaum** named as sole owner of secrets, backups, domain, and incident response — closing the open owner/region items (backup/restore-test cadence and incident runbook still to be written down). **H-05:** offline-manifest thresholds (15-min current / 4-hr aging) and retention (earlier of 14 days after save or 7 days after trip end) approved as-is, with the missing/expired/corrupt-copy stop rule recorded; still gated on V-02 field evidence. **H-12 (retained):** re-attached an explicit legal/tax review requirement to the closed row and `marketing.md` — taxes/fees are jurisdiction-dependent and the published two-year price lock and same-day support promise are binding commercial commitments that closing the *price* did not clear. | Product owner |
| 2026-07-24 | Added the per-discipline [stakeholder playbooks](stakeholders/README.md) (legal, insurance, finance/tax, privacy/communications, dive operations, commercial/industry) and registered **H-18** (corporate footing: Pseudorandom LLC vs. a dedicated DiveDay entity, plus the pilot/subscription/privacy contract set) and **H-19** (DiveDay's own E&O/cyber coverage). Recorded the owner's planning assumptions of 2026-07-24: Florida as the launch jurisdiction H-01 plans around (final choice still recorded with counsel), and the Pseudorandom LLC context for H-18. | Product owner + Engineering |
| 2026-07-24 | Split the legal engagement across two attorneys, per owner preference: default to the owner's existing generalist attorney (already engaged) and escalate to a scuba-liability specialist (not yet found) only where the exposure is genuinely dive-specific, with room to upgrade a generalist item to specialist review if it turns out closer to the line than expected. Added [stakeholders/legal-engagement-scope.md](stakeholders/legal-engagement-scope.md) — the tiered scope, written as a send-ready brief for the generalist — and updated `stakeholders/legal.md`, `stakeholders/README.md`, and the H-01/H-02/H-03/H-17/H-18 "human owner" cells to point at it. No H-row status changed; this only clarifies who does which part of the already-registered work. | Product owner |
| 2026-07-24 | Recorded the owner-actionable policy decisions (first of a four-slice build off the confirmed suggestions). **H-07:** no hard deposit/window defaults, but a guidance hint on the trip form ("20–30% of the fare"; "48 hours"), flat deposits, no platform fee, no unpaid-booking auto-expire, Connect credential owner = Aaron; taxes still gated on the accountant. **H-12:** billing is month-to-month/per-location/cancel-anytime, stated in a pricing FAQ; taxes/contract/price-lock stay gated on the legal/tax reviewer. **H-08:** the conservative course baseline is approved as the operating starting point; the per-agency ratios/depth/age/medical matrix still needs the operator's agency-specific input to encode. **H-06:** measurement capture confirmed as a planning-input-only request; substitution authority and the "needs staff fit" fallback are decided and being built on top of the H-14 role matrix. **H-09:** sender = the shop's brand (already so), the existing transactional templates are approved as launch copy (the template library already shipped as the mechanism — nothing new to build), email is transactional so needs no marketing opt-in, and Aaron owns the Resend/cron/Twilio credentials; SMS/WhatsApp consent stays gated on the attorney. | Product owner + Engineering |
| 2026-07-24 | **H-08 ratios resolved 2026-07-24 (Aaron Buxbaum): use PADI/SSI's own published standards directly** rather than waiting on operator-specific input, since the figures are consistent enough across agencies to trust from public sources. Shipped the entry-level (DSD/Open Water) in-water ratio — 8 students per instructor, +2 per certified assistant, ceiling 12 — as a real booking gate (`src/lib/course-ratios.ts`), scoped to ungated course sessions only; continuing-ed courses stay unratioed, matching PADI's own published scope. Recorded depth-ceiling and minimum-age/Junior standards as documented reference data (glossary, [20260724-course-admission-standards](../architecture/decisions/20260724-course-admission-standards.md), sourced/cited) — not yet enforced, since DiveDay stores neither numeric site depth nor diver date of birth; flagged the DOB/fail-open-vs-fail-closed question as its own scoped follow-up rather than deciding it silently. Pending `dive-domain-expert` review before merge (safety-critical). | Product owner + Engineering |
| 2026-07-24 | Shipped the last two build items off the confirmed suggestions. **H-06 → Implemented:** the gear-request override gate (`canOverrideGearRequest` — owner/manager/instructor/divemaster, deliberately wider than the trip-config gate since sizing is in-water judgement) and the **"needs staff fit"** fallback, a first-class state that blanks a diver's *sizes* on the prep list — keeping their count, so the boat still loads a unit to fit them from — rather than letting the crew quietly pack a substitute; raising the flag stays open to all staff because it escalates to a person instead of overwriting the diver's request, while clearing it is gated. **H-08 minimum age:** owner chose **option B** — collect an optional diver date of birth and enforce a course's `minimum_age` only when one is on file, measured on the course date. Fail-open by design: existing divers book unchanged, and option C (fail closed) was declined for its ship-day blast radius but stays a later upgrade. Depth ceilings remain documented-but-unenforced pending a structured site depth. New ADR [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md); glossary gained "Needs staff fit", "Gear-request override", and "Date of birth". | Product owner + Engineering |
| 2026-07-24 | Registered and implemented **H-20** (owner request): the contact importer now lands imported certification and nitrox cards as `verified` and permanently flagged `imported`, surfaced for a soft one-tap staff confirm, for all sources including spreadsheets — reversing [20260723-contact-importer](../architecture/decisions/20260723-contact-importer.md)'s "claimed, never verified" rule. The assistant flagged the safety edge (imported cards clear depth gates and authorize nitrox fills on a CSV cell) and its safer alternatives before implementing; the owner chose "verified + soft confirm." Same decision: PDF import documents (magic-byte-routed, stored as-is) and the bidirectional concierge switch offer on every switching page. Card expiry, no-number-no-card, and never-reconstruct-medical-answers all still hold. Pending `dive-domain-expert` + `security-reviewer` review before merge (safety-critical). See [20260724-import-verified-cards](../architecture/decisions/20260724-import-verified-cards.md). | Product owner + Engineering |
| 2026-07-25 | Registered **H-21** and **H-22**, raised by review findings on the merged H-06/H-08 gear-fit and age-gate work rather than decided as part of it. **H-21:** the minimum-age gate made age a stored, enforced fact with no surface for it — the roster/manifest show no minor indicator, and a minor can sign their own waiver solo through `/waivers/[token]` with no guardian-signature step. **H-22:** confirm whether the shipped public-booking behavior (never refuse on age, identical blocker copy to the identity-mismatch case so the diver-facing checklist discloses nothing) is the intended shape of the approved "option B," since the fail-open decision didn't itself specify the public-path disclosure boundary — a `security-reviewer` pass added that scope to close an exploitable-oracle finding. | Engineering |
| 2026-07-25 | Registered and implemented **H-23** (owner request): move rows out of the honesty table's "Stays behind" column. **Specialty cards now come across** — `verified` and flagged imported like a level card, with one deliberate divergence the assistant raised and the owner chose: the specialty *gate* holds until a staffer taps the one-tap confirm, because a specialty authorizes a materially riskier dive (deep gates depth past 18 m) and importing specialties removes the "specialties stay gated" mitigation H-20 leaned on. Boarding never waits. Same change: card **expiry** now travels with an imported card (a past date lands as expired rather than valid forever — a real pre-existing gap) and **dive insurance** comes across as free text, closing an export/import asymmetry. Payment methods and booking/service history stay behind by design. Pending `dive-domain-expert` + `security-reviewer` review before merge (safety-critical). See [20260725-import-specialty-cards](../architecture/decisions/20260725-import-specialty-cards.md). | Product owner + Engineering |
| 2026-07-25 | **H-22 decided + shipped (Aaron Buxbaum): name the real age-gate reason on the diver's own checklist.** The assistant flagged that the row's own suggested gate — disclose more "once `identityUnconfirmedAt` clears" — doesn't actually work, since that flag only catches a *mismatched* name and a targeted attacker who already knows the child's real name never trips it; the owner chose to proceed with that gate anyway, accepting the documented gap for the UX gain in the common case. New ADR [20260725-checklist-age-disclosure](../architecture/decisions/20260725-checklist-age-disclosure.md); glossary's "Date of birth" entry and the [20260724-gear-fit-fallback](../architecture/decisions/20260724-gear-fit-fallback.md) ADR updated to point at it. | Product owner + Engineering |
| 2026-07-25 | Registered and implemented **H-24** (owner request, following the H-23 review): the two findings that specialty-card import left open are now closed. The gate-opening confirms (imported specialty, imported nitrox) require an explicit **card sighting** and record it, so the tap that opens a depth gate or authorizes a fill states what the staffer is asserting — matching the paper waiver's medical attestation, and with no bulk confirm. And the importer no longer bends a **technical rating** onto the recreational ladder: Advanced Nitrox, Trimix, CCR, cave, deco, and Tec ratings import as nothing and are named as such in the preview, instead of landing as a verified Advanced Open Water card. See [20260725-imported-card-sighting](../architecture/decisions/20260725-imported-card-sighting.md). | Product owner + Engineering |
| 2026-07-26 | **H-05 revised + shipped (Aaron Buxbaum): consolidate the manifest's offline controls and automate the device copy.** The live manifest page carried three controls (save-or-refresh, open offline copy, delete device copy) for a task the owner felt should mostly run itself. The device snapshot now saves and refreshes automatically (on load, reconnect, tab focus, and a five-minute interval) with one remaining manual "Refresh now" button; the "Delete device copy" affordance is removed from both the live manager and the offline viewer. Retention (earlier of 14 days after save or 7 days after trip end) stays as originally approved — the owner asked to keep the 7-day leg rather than shorten it. Push-based refresh (WebSocket/SSE instead of polling) was considered and deliberately deferred as its own ADR-worthy infrastructure decision. `dive-domain-expert` review of this change surfaced two issues fixed in the same PR: removing the delete button turned a corrupt/undecryptable local record into a permanent dead end (a save that reads a bad existing record now self-heals instead of aborting) and lazy expiry could have silently discarded an offline roll-call event that never reached the server (expiry now waits for pending events to resolve first). See [20260726-manifest-offline-copy-automation](../architecture/decisions/20260726-manifest-offline-copy-automation.md). | Product owner + Engineering |
| 2026-07-30 | Recorded three product-owner decisions. **H-21 (Chosen, not yet implemented):** the roster/manifest will show age in years (when known) and a minor indicator, plus a birthday/upcoming-birthday celebratory callout alongside the existing dive-count milestones; solo waiver signature for minors is accepted as-is for now but explicitly flagged for review alongside H-01–H-03 rather than left silently open. **H-19 (Deferred):** DiveDay goes without its own E&O/cyber coverage until a pilot is closer to real, then revisits with a broker once H-18 (entity) is settled. **H-08 depth ceilings (decided, not yet implemented):** `dive_sites` gains a structured numeric depth field; a site deeper than a diver's certification level supports (including Junior age bands) shows staff a warning, not a block, so an instructor can deliberately keep a diver shallower than the site max. **H-11 (Implemented):** the provisional nitrox fill-station parameters (22–40% O₂, ppO₂ 1.4/1.6, verified-card gate) are approved as the operating policy as-is; still gated on V-05 review before a real fill station operates. | Product owner |
| 2026-07-30 | Recorded three more product-owner decisions. **H-01:** confirmed Florida as the launch jurisdiction (was a planning assumption only); row stays Ready pending the Tier 3 specialist's waiver-text/medical-question review. **H-02:** working default of indefinite waiver/medical retention, recorded for the attorney/specialist review rather than left unstated; row stays Ready. **H-07:** reconfirmed no baked-in default deposit or cancellation-window values — the guidance hint is the permanent answer, not an interim step. | Product owner |
| 2026-07-30 | **Shipped H-08 depth ceilings and H-21 minor/birthday surfaces**, the two decisions recorded earlier the same day. `dive_sites.max_depth_meters` (canonical metres, floating point so a feet entry round-trips exactly) is compared against a diver's certification ceiling and junior age band in `src/lib/depth-ceiling.ts`; the result rides `listTripReadiness` as `depthAdvisory`, a **sibling of `readiness` that can never become a blocker** — an instructor may deliberately keep a diver shallower than the site's maximum, so it warns and the crew decides. The trip's depth is the deepest site it visits, across the primary site *and* every ordered dive. H-21 puts age in years, a **Minor** badge (18 — legal majority for the guardian-signature question, not the diving world's 15, which is handled separately by the junior depth bands) and a birthday callout on the roster and the boat manifest, plus a `Celebrations` section above the roster. **Two clarifications the product owner made during the build:** the birthday window is **seven days in either direction** (a Tuesday birthday still counts on Saturday's boat) and its copy is the icon plus the timing only — "🎂 in 2d", "🎂 2d ago". H-21 had described the callout as "similar to the existing dive-count milestone"; no such section exists in the codebase, so the shape was confirmed rather than guessed. **Also added on the owner's request mid-build:** `shops.depth_unit`, a settings toggle between metres and feet — display and entry only, since storage stays metres, so flipping it moves no stored number. Solo waiver signature by a minor remains possible and stays flagged for the H-01–H-03 legal review; surfacing the badge makes the gap visible to staff but does not close it. New ADR [20260730-site-depth-and-diver-age-surfaces](../architecture/decisions/20260730-site-depth-and-diver-age-surfaces.md); glossary gained "Site maximum depth", "Depth ceiling", "Depth unit", and "Minor". | Product owner + Engineering |
| 2026-08-02 | **Owner request: swap the dormant Twilio-backed courtesy-SMS channel for AWS SNS**, reusing the AWS account/IAM conventions already stood up for SES email rather than onboarding a second vendor for a channel that was never actually configured. `src/lib/notifications/sms.ts`'s `SmsProvider` seam is unchanged from the caller's side; internally it now calls SNS's `Publish` API via `@aws-sdk/client-sns` instead of a fetch-based Twilio POST. WhatsApp is dropped from the type entirely (not just left unwired) since AWS SNS has no WhatsApp delivery path and nothing ever called it. New ADR [20260802-sns-sms-adapter](../architecture/decisions/20260802-sns-sms-adapter.md), superseding [20260721-sms-whatsapp-notifications](../architecture/decisions/20260721-sms-whatsapp-notifications.md). No H-row status changed; H-09 and H-18 updated only to name SNS instead of Twilio. | Product owner + Engineering |
